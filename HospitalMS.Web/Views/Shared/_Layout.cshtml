<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Hospital Management System</title>
    <meta name="description" content="Professional Hospital Management System — Streamlined healthcare operations, appointment scheduling, and patient care." />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/site-custom.css" asp-append-version="true" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-light fixed-top navbar-glass">
            <div class="container-fluid px-4">
                <a class="navbar-brand d-flex align-items-center" asp-controller="Home" asp-action="Index">
                    <i class="bi bi-hospital-fill me-2 fs-4"></i>
                    <span>Hospital MS</span>
                </a>
                <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    @if (User.Identity?.IsAuthenticated == true)
                    {
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0 align-items-lg-center">
                            @if (User.IsInRole("Admin"))
                            {
                                <li class="nav-item">
                                    <a class="nav-link" asp-controller="Admin" asp-action="Dashboard">
                                        <i class="bi bi-speedometer2 me-1"></i> Dashboard
                                    </a>
                                </li>
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="mgmtDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                        <i class="bi bi-people me-1"></i> Management
                                    </a>
                                    <ul class="dropdown-menu border-0 shadow" aria-labelledby="mgmtDropdown">
                                        <li>
                                            <a class="dropdown-item" asp-controller="Doctor" asp-action="Index">
                                                <i class="bi bi-person-badge me-2 text-primary"></i>Manage Doctors
                                            </a>
                                        </li>
                                        <li>
                                            <a class="dropdown-item" asp-controller="Patient" asp-action="Index">
                                                <i class="bi bi-people me-2 text-primary"></i>Manage Patients
                                            </a>
                                        </li>
                                        <li>
                                            <a class="dropdown-item" asp-controller="Department" asp-action="Index">
                                                <i class="bi bi-building me-2 text-primary"></i>Departments
                                            </a>
                                        </li>
                                    </ul>
                                </li>
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="clinicalDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                        <i class="bi bi-heart-pulse me-1"></i> Clinical
                                    </a>
                                    <ul class="dropdown-menu border-0 shadow" aria-labelledby="clinicalDropdown">
                                        <li>
                                            <a class="dropdown-item" asp-controller="MedicalRecord" asp-action="Index">
                                                <i class="bi bi-file-medical me-2 text-info"></i>Medical Records
                                            </a>
                                        </li>
                                    </ul>
                                </li>
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="financeDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                        <i class="bi bi-cash-coin me-1"></i> Financial
                                    </a>
                                    <ul class="dropdown-menu border-0 shadow" aria-labelledby="financeDropdown">
                                        <li>
                                            <a class="dropdown-item" asp-controller="Billing" asp-action="Index">
                                                <i class="bi bi-receipt me-2 text-success"></i>Billing &amp; Reports
                                            </a>
                                        </li>
                                    </ul>
                                </li>
                            }
                            @if (User.IsInRole("Doctor"))
                            {
                                <li class="nav-item">
                                    <a class="nav-link" asp-controller="Doctor" asp-action="Dashboard">
                                        <i class="bi bi-grid-fill me-1"></i> Dashboard
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link position-relative" asp-controller="Appointment" asp-action="PendingApprovals">
                                        <i class="bi bi-bell me-1"></i> Requests
                                        <span id="badge-requests" class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" style="font-size:0.6rem; display:none;">
                                            0
                                        </span>
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" asp-controller="Appointment" asp-action="Index">
                                        <i class="bi bi-calendar-check me-1"></i> Schedule
                                    </a>
                                </li>
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="doctorDropdown" role="button" data-bs-toggle="dropdown">
                                        <i class="bi bi-briefcase me-1"></i> Practice
                                    </a>
                                    <ul class="dropdown-menu border-0 shadow">
                                        <li>
                                            <a class="dropdown-item" asp-controller="Doctor" asp-action="ManageWorkingHours">
                                                <i class="bi bi-clock me-2"></i>Availability
                                            </a>
                                        </li>
                                        <li>
                                            <a class="dropdown-item" asp-controller="Appointment" asp-action="History">
                                                <i class="bi bi-clock-history me-2"></i>History
                                            </a>
                                        </li>
                                        <li><hr class="dropdown-divider"></li>
                                        <li>
                                            <a class="dropdown-item" asp-controller="MedicalRecord" asp-action="Index">
                                                <i class="bi bi-file-medical me-2 text-info"></i>Medical Records
                                            </a>
                                        </li>
                                    </ul>
                                </li>
                            }
                            @if (User.IsInRole("Patient"))
                            {
                                <li class="nav-item">
                                    <a class="nav-link" asp-controller="Patient" asp-action="Dashboard">
                                        <i class="bi bi-house-door me-1"></i> Home
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" asp-controller="Appointment" asp-action="Index">
                                        <i class="bi bi-journal-medical me-1"></i> My Appointments
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" asp-controller="Billing" asp-action="Index">
                                        <i class="bi bi-receipt me-1"></i> Billing & Reports
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" asp-controller="MedicalRecord" asp-action="Index">
                                        <i class="bi bi-file-medical me-1"></i> Medical Records
                                    </a>
                                </li>
                                <li class="nav-item ms-lg-2">
                                    <a class="btn btn-sm btn-primary rounded-pill px-3 shadow-sm" asp-controller="Doctor" asp-action="Index">
                                        <i class="bi bi-plus-lg me-1"></i> Book Appointment
                                    </a>
                                </li>
                            }
                        </ul>
                    }
                    <ul class="navbar-nav ms-auto align-items-center">
                        @if (User.Identity?.IsAuthenticated == true)
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle d-flex align-items-center py-0" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                                    <div class="text-end me-2 d-none d-lg-block" style="line-height:1.2;">
                                        <span class="d-block fw-bold text-dark small">@User.Identity.Name</span>
                                        <span class="d-block text-muted" style="font-size:0.7rem;">
                                            @(User.IsInRole("Admin") ? "Administrator" : User.IsInRole("Doctor") ? "Medical Staff" : "Patient")
                                        </span>
                                    </div>
                                    <div class="position-relative d-inline-block">
                                        <div class="avatar-circle bg-primary text-white rounded-circle d-flex align-items-center justify-content-center fw-bold" style="width:38px;height:38px;">
                                            @(User.Identity.Name?.Substring(0, 1).ToUpper() ?? "U")
                                        </div>
                                        <span id="connection-indicator"
                                              class="connection-indicator disconnected"
                                              title="Disconnected from real-time notifications">
                                        </span>
                                    </div>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end border-0 shadow m-0">
                                    <li>
                                        <a class="dropdown-item" asp-controller="Account" asp-action="Profile">
                                            <i class="bi bi-person me-2"></i> My Profile
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" asp-controller="Account" asp-action="ChangePassword">
                                            <i class="bi bi-shield-lock me-2"></i> Security
                                        </a>
                                        <a class="dropdown-item" href="#" id="dark-mode-toggle">
                                            <i class="bi bi-moon me-2"></i> Toggle Dark Mode
                                        </a>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                                            <button type="submit" class="dropdown-item text-danger w-100 text-start">
                                                <i class="bi bi-box-arrow-right me-2"></i> Sign Out
                                            </button>
                                        </form>
                                    </li>
                                </ul>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link fw-bold" asp-controller="Account" asp-action="Login">Log In</a>
                            </li>
                            <li class="nav-item ms-2">
                                <a class="btn btn-primary rounded-pill px-4 fw-bold" asp-controller="Account" asp-action="Register">Get Started</a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <div class="toast-container p-3" id="toastContainer" style="top: 70px; right: 20px; z-index: 9999;"></div>

    <main role="main" class="pb-5" style="margin-top: 76px; min-height: calc(100vh - 140px);">
        @RenderBody()
    </main>

    <footer class="py-4 mt-auto">
        <div class="container text-center text-muted small">
            <p class="mb-1"><i class="bi bi-hospital-fill me-1 text-primary"></i> Hospital Management System</p>
            <p class="mb-0 opacity-75">&copy; @DateTime.Now.Year All rights reserved. Crafted with <i class="bi bi-heart-fill text-danger" style="font-size:0.65rem;"></i> for better healthcare.</p>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/lib/microsoft-signalr/dist/browser/signalr.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @if (User.Identity?.IsAuthenticated == true)
    {
        <input type="hidden" id="current-user-role" value="@User.FindFirst(System.Security.Claims.ClaimTypes.Role)?.Value" />
        <input type="hidden" id="current-user-id" value="@User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value" />
        <script src="~/js/notifications.js" asp-append-version="true"></script>
    }
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            var currentPath = window.location.pathname.toLowerCase();
            document.querySelectorAll('.navbar-nav .nav-link').forEach(function(link) {
                var href = link.getAttribute('href');
                if (href && href.toLowerCase() === currentPath) {
                    link.classList.add('active');
                }
            });
            var successMsg = '@TempData["SuccessMessage"]';
            var errorMsg = '@TempData["ErrorMessage"]';
            var infoMsg = '@TempData["InfoMessage"]';
            var warningMsg = '@TempData["WarningMessage"]';
            if (successMsg) showToast(successMsg, 'success');
            if (errorMsg) showToast(errorMsg, 'danger');
            if (infoMsg) showToast(infoMsg, 'info');
            if (warningMsg) showToast(warningMsg, 'warning');
        });
        function showToast(message, type) {
            var icon = type === 'success' ? 'bi-check-circle-fill' :
                       type === 'danger' ? 'bi-exclamation-octagon-fill' :
                       type === 'warning' ? 'bi-exclamation-triangle-fill' : 'bi-info-circle-fill';
            var color = type === 'success' ? 'bg-success' :
                        type === 'danger' ? 'bg-danger' :
                        type === 'warning' ? 'bg-warning text-dark' : 'bg-info';
            var toastHtml = `
                <div class="toast align-items-center text-white ${color} border-0 mb-2" role="alert" aria-live="assertive" aria-atomic="true">
                    <div class="d-flex">
                        <div class="toast-body">
                            <i class="bi ${icon} me-2"></i> ${message}
                        </div>
                        <button type="button" class="btn-close ${type === 'warning' ? '' : 'btn-close-white'} me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
                    </div>
                </div>`;
            var container = document.getElementById('toastContainer');
            if(container) {
                container.insertAdjacentHTML('beforeend', toastHtml);
                var toastEl = container.lastElementChild;
                var toast = new bootstrap.Toast(toastEl, { delay: 5000 });
                toast.show();
            }
        }
    </script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>